services:
  - type: web
    name: maof-engine
    runtime: python
    rootDir: maof-engine
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHONUTF8
        value: "1"
    plan: free
